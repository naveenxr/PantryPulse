const cron = require("node-cron");

/**
 * Food Status Job
 * Scheduled job to check item expiration statuses daily.
 */
const startFoodStatusJob = () => {
  // Cron schedule will be defined here
};

module.exports = { startFoodStatusJob };
